apiVersion: v1
kind: Service
metadata:
  name: {{ include "ep-digital-common.fullname" . }}
  labels:
    {{- include "ep-digital-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ep-digital-common.selectorLabels" . | nindent 4 }}
